{
  "name": "matrix-threadbot",
  "version": "0.1.0",
  "description": "bot for matrix chat threads",
  "main": "./src/index.ts",
  "scripts": {
    "start": "yarn build && node build/index.js",
    "start:dev": "yarn nodemon",
    "build": "rimraf ./build && tsc"
  },
  "keywords": [
    "matrix",
    "chat",
    "thread",
    "bot"
  ],
  "author": "woei",
  "license": "MIT",
  "dependencies": {
    "matrix-js-sdk": "^26.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "dotenv": "^16.3.1",
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}
